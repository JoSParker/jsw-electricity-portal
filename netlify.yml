# netlify.yml configuration file
build:
  publish: dist/jsw/browser
  command: npm ci && npm run build -- --configuration production

# Handle all routes for SPA
redirects:
  - from: "/*"
    to: "/index.html"
    status: 200
    force: true
